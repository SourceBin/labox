#!/bin/sh

addgroup -g 1000 labox
adduser -u 1000 -G labox -s /bin/sh -D labox

chown labox:labox -R /home/labox

echo "http://dl-cdn.alpinelinux.org/alpine/edge/testing" >> /etc/apk/repositories
apk add --update --no-cache \
  <%- basePackages.join(' \\\n  ') %>
